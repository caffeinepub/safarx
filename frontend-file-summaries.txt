{
  "frontend/index.html": "HTML entry point with the Google Analytics 4 gtag.js snippet (Measurement ID: G-73VJ1TQ3D7) inserted as the first child of `<head>`, followed by all existing SEO meta tags, Open Graph, Twitter Card, Google Fonts, and Google Sign-In script tags.",
  "frontend/src/index.css": "Global stylesheet with OKLCH color system using warm earthy palette (saffron, terracotta, teal, ivory), custom utility classes for hero overlays, full-bleed sections, card hover effects, and typography setup.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families (Playfair Display, Cormorant Garamond, Inter), full saffron/terracotta/teal/ivory color scales, custom shadows, and fade animations.",
  "frontend/src/data/destinations.ts": "Static TypeScript data file exporting 20+ curated Indian destinations with id, name, region, state, descriptions, travel info, highlights, and image paths.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks file with all backend operations; admin-gated hooks (useGetAllInquiries, useCommunityStats, useCommunityUserList) now use a session token from localStorage (key: 'adminSession') instead of Internet Identity for their enabled guard; useDeleteInquiry passes the token as the second parameter; useLoginAdmin and useRegisterAdmin call the actual backend actor methods and store the returned session token in localStorage.",
  "frontend/src/components/Header.tsx": "Responsive fixed site header with SafarX.in branding and full navigation — z-index lowered from `z-50` to `z-40` so that modal dialogs (which use `z-50`) always render above the header without being obscured.",
  "frontend/src/components/Footer.tsx": "Site-wide footer with brand info, region links, quick links, contact details, and social icons — LinkedIn URL updated to `https://www.linkedin.com/company/safarx-in/?viewAsMember=true` in both the social icons row and the \"Get In Touch\" contact section, and Instagram handle updated to `@travel_safarx`.",
  "frontend/src/components/DestinationCard.tsx": "Reusable destination card component with image, tag badge, region badge, name, short description, and a link to the detail page.",
  "frontend/src/components/HeroSection.tsx": "Reusable full-viewport hero section with background image, headline, subtitle, CTA buttons, decorative overlay, and scroll indicator.",
  "frontend/src/pages/Home.tsx": "Homepage with hero, stats, featured destinations, promo section, why-choose-us cards, and CTA banner — now passes full Open Graph and Twitter Card values (ogType: website, hero Himalaya image) to useSEO alongside existing title/description and TravelAgency JSON-LD.",
  "frontend/src/pages/Destinations.tsx": "Destinations listing page with region filter tabs, search input, and responsive destination grid — now passes full Open Graph and Twitter Card values (destinations banner image, ogUrl for /destinations) to useSEO alongside existing title/description and BreadcrumbList JSON-LD.",
  "frontend/src/pages/DestinationDetail.tsx": "Individual destination detail page — now passes dynamic per-destination Open Graph and Twitter Card values (destination name/state as ogTitle, destination image as ogImage, ogType: article, canonical destination URL) to useSEO.",
  "frontend/src/pages/Contact.tsx": "Contact/inquiry page with form and contact info sidebar — now passes full Open Graph and Twitter Card values (SafarX logo image, twitterCard: summary, ogUrl for /contact) to useSEO alongside existing title/description.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup split into two layout groups — a public layout (with HeaderWithAdmin and Footer) for all non-admin routes, and a bare admin layout (no header/footer) for `/admin` and `/admin/login` routes.",
  "frontend/src/pages/Admin.tsx": "Admin dashboard that uses the localStorage session token to gate access and load data — fixed import names to use `useCommunityStats`, `useCommunityUserList`, and `clearAdminSessionToken` matching the actual exports in useQueries.ts.",
  "frontend/src/components/HeaderWithAdmin.tsx": "Responsive site header with SafarX.in branding, now including a Community nav link (with Users icon) between Packages and Contact in both desktop nav and mobile drawer.",
  "frontend/src/pages/AdminLogin.tsx": "Two-step admin login/register page — now calls useSEO with title \"Admin Login\" and a 140-160 char description indicating this is the secure administrative access portal for SafarX.",
  "frontend/src/pages/Community.tsx": "Community page with hero banner, Feed/My Profile tabs, and auth modal — guards the `CommunityProfile` render with a null check on `session`, showing a login prompt when the user is not authenticated, and removes the invalid `onLoginRequest` prop that `CommunityProfile` does not accept.",
  "frontend/src/components/CommunityAuthModal.tsx": "Community auth modal with Google Sign-In and username/password flows — now includes the user's `password` in the session object passed to `onLoginSuccess` for both Google users (`'GOOGLE_AUTH'`) and regular users (their actual password), enabling downstream components to authorize backend operations without re-prompting.",
  "frontend/src/components/CommunityFeed.tsx": "Community feed component displaying all travel posts as cards — replaced deprecated `flex-shrink-0` with `shrink-0` on the author avatar and destination badge elements.",
  "frontend/src/components/CreatePostModal.tsx": "Modal form for creating new community travel posts — fixed the TypeScript error by passing the required `password` field to `createPost.mutateAsync`, using `'GOOGLE_AUTH'` for Google users and `session.password` for regular users.",
  "frontend/src/components/CommunityProfile.tsx": "My Profile view updated to display a Gmail Account badge and email address for Google-authenticated users, with a `ShieldCheck` icon indicator, and simplified delete flow for Google users (no password prompt needed).",
  "frontend/src/types/google-gsi.d.ts": "TypeScript declaration file that types the `window.google.accounts.id` GSI API surface, including `initialize`, `prompt`, `renderButton`, credential response, and related interfaces.",
  "frontend/.env.example": "Environment variable template documenting the required `VITE_GOOGLE_CLIENT_ID` variable with instructions for obtaining a Google OAuth 2.0 Client ID from the Google Cloud Console.",
  "frontend/src/hooks/useSEO.ts": "Extended custom React hook that accepts `title`, `description`, plus optional Open Graph (`ogTitle`, `ogDescription`, `ogImage`, `ogUrl`, `ogType`) and Twitter Card (`twitterCard`, `twitterTitle`, `twitterDescription`, `twitterImage`) parameters; dynamically injects or updates corresponding meta tags in the document head and removes them on unmount, fully backward-compatible with existing usage.",
  "frontend/src/utils/structuredData.ts": "Utility module exporting getTravelAgencySchema() (schema.org TravelAgency JSON-LD object for SafarX) and getBreadcrumbSchema() (BreadcrumbList JSON-LD generator accepting an array of name/url items).",
  "frontend/src/pages/Packages.tsx": "Travel packages listing page with category filter and packages grid — now passes full Open Graph and Twitter Card values (packages hero banner image, ogUrl for /packages) to useSEO alongside existing title/description and BreadcrumbList JSON-LD.",
  "frontend/src/pages/PlanTrip.tsx": "AI-powered trip planner page with destination/duration/style/group form and itinerary timeline — now passes full Open Graph and Twitter Card values (itinerary planner hero image, ogUrl for /plan) to useSEO alongside existing title/description.",
  "frontend/public/sitemap.xml": "Static XML sitemap listing all public SafarX pages including home, destinations, packages, plan-trip, community, contact, and 22 individual destination detail pages, each with loc, lastmod, changefreq, and priority elements using the canonical domain https://safarx.in.",
  "frontend/public/robots.txt": "Robots.txt file allowing all user-agents to crawl the entire site and pointing crawlers to the sitemap at https://safarx.in/sitemap.xml."
}